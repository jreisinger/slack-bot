{
  "name": "slack-bot",
  "version": "0.0.1",
  "description": "learning slack bots",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "jr",
  "license": "ISC",
  "dependencies": {
    "@slack/client": "^3.10.0"
  }
}
